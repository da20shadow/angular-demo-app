const table = `w-full bg-white dark:bg-gray-800
text-center dark:shadow-gray-500 overflow-hidden  rounded-md shadow-sm `;

const headRow = '';

const bodyRow = '';

export const tasksTableStyles = {
  table,
  headRow,
  bodyRow,
}

